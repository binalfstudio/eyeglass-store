[build]
builder = "nixpacks"
buildCommand = "npm install && npm install --prefix client/eyeglass && npm run build"

[deploy]
startCommand = "node server/utils/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
